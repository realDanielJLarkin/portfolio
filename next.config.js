/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['headlessportfolio.local', 'portfolio.stepoutandgo.com']
  }
}

module.exports = nextConfig
